import { TActivityDifficulty } from "./genLesson";

const genRearrangingActivity = (
  verseText: string,
  difficulty: TActivityDifficulty
) => {
  // need function to separate text into chunks
};

export default genRearrangingActivity;
